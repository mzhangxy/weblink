# StackHost configuration file

user:
  name: jacob
  github: mzhangxy/weblink

runtime:
  image: node:22

commands:
  package: ""
  build:
     - npm install 
  start: node index.js

repository:
  branch: main
  auto_deploy: true 

env:
